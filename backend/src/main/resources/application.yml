spring:
  application:
    name: teamflow-backend

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${SERVER_PORT:8080}

app:
  name: TeamFlow API
  version: 0.1.0
  environment: development
